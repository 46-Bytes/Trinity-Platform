# Development and test dependencies.
# Installs everything in requirements.txt, plus tooling that should not ship
# to production. Use this instead of requirements.txt when working locally:
#     pip install -r requirements-dev.txt
-r requirements.txt

# Testing
pytest==8.3.4
